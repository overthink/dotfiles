# Stuff I normally add to my .bashrc
# vim: ft=sh
set -o vi

export EDITOR="vim"

# 256 colour terminals: the future is now
if [ -n "$DISPLAY" -a "$TERM" == "xterm" ]; then
    export TERM=xterm-256color
fi

if [ -e ~/.bashrc.local ]; then
  . ~/.bashrc.local
fi

PATH=$PATH:~/.cabal/bin

